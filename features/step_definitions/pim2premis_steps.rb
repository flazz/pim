
Given /^a PREMIS\-in\-METS document$/ do
  pending
end

Then /^a PREMIS document should be returned$/ do
  pending
end

Given /^a PREMIS\-in\-METS document with PREMIS embedded as a container$/ do
  pending
end

Given /^a PREMIS\-in\-METS document with PREMIS embedded as buckets$/ do
  pending
end

Then /^it should reference a stylesheet$/ do
  pending
end

Given /^it has multiple structure maps$/ do
  pending
end

Then /^a choice of potential structural representations will be returned$/ do
  pending
end
